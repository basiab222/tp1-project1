package tp1.logic.lists;

import tp1.logic.gameobjects.Bomb;
import tp1.logic.gameobjects.DestroyerAlien;

public class BombList {
    /*private Bomb[] bombList;
    private int counter;

    public BombList(int destroyerAlienNumbers) {
        this.bombList = new Bomb[destroyerAlienNumbers];
        this.counter = 0;
    }

    public void addBomb(Bomb bomb, DestroyerAlien shooter){
            bombList[counter] = bomb;
            counter++;
    }

    public Bomb getBomb(int index) {
        if (index >= 0 && index < counter) {
            return bombList[index];
        }
        return null;
    }

    public void removeBomb(int index) {
        if (index >= 0 && index < counter) {
            for (int i = index; i < counter - 1; i++) {
                bombList[i] = bombList[i + 1];
            }
            counter--;
            bombList[counter] = null;
        }
    }

    public int getCounter() {
        return counter;
    }*/
}